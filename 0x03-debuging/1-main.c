#include <stdio.h>
/**
 * main - causes an infinite loop
 * Return: always 0
 */
int main(void)
{
	int i;
		printf("Infinite loop incoming :(\n");
	i = 0;
	/**
	*while (i < 10)
	*{
	*	_putchar(i);
	*}
	*/
	prinf("Infinite loop avoided! \\0/\n");

	return (0);
}
